When the tool can be deployed according to easily available instructions such that when it runs it allows interesting calculations to be easily requested, returns the results of those calculations in a readable format, and supports authorization so that not just anybody with an OpenID can login and change everything, then this tikcet can be closed.
